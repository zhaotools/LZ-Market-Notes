# 本地检查结果

验证日期：2026-09-11。

Node 数据、转义、同步边界、YouTube RSS、公众号导入、二维码路径与正式发布配置测试：18 项通过。

Chrome 界面检查：170 项通过，运行时 JavaScript 错误为 0。

包括六个页面的主内容、当前导航与页面标题，5 篇正式公众号文章及链接、文章搜索、430×430 二维码加载、4 个有效公开视频、播放器按需创建与关闭、16 个 Map 公开资产、市场筛选、Map/工具箱保持独立外链、弹窗、手机菜单，以及在 360/390/430/768/1024/1440px 六种宽度下所有页面无横向溢出。另检查了独立 `preview.html` 的页面切换和正式文章目录。

自动更新包含 YouTube 最近 5 条上限、明确排除无效视频 ID，以及公众号 RSS/Atom 连接器的校验测试；本次共 22 项 Node 测试通过。公众号连接器只接受账号名称匹配、包含有效日期的 `mp.weixin.qq.com` 原文链接，并且不会保存正文。联网执行同步时会保留失败前的已有数据；未配置 `WECHAT_FEED_URL` 时公众号目录保持不变并正常退出。

现有截图来自设计基线；本轮没有修改页面样式或布局文件。

## 验证边界

本轮通过本地 HTTP 服务加载六个独立页面，并通过 `file://` 打开独立预览进行交互检查。

联网检查已确认 5 个公众号原文页面的公众号名、标题、日期与页面摘要，以及 Map 公开 JSON、YouTube RSS、公开视频页面、缩略图和 oEmbed。2026-09-11 对 `1YV4RAFeNXs` 的页面元数据复核确认其为无法播放的离线直播占位页，已从网站目录及后续 RSS 同步中排除。未测试 Data API 完整目录、Safari、微信内置浏览器、真实网络稳定性或完整无障碍符合性。

详细检查条目见 browser-checks.json。
